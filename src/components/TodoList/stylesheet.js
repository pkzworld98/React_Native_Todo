import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  container: {
    borderWidth: 3,
    borderColor: 'red',
    marginTop: 20,

    width: '88%',
    height: '60%',
    maxHeight: '60%',
  },
});
